# Near public releases

This repository contains public binary distribution assets for Near Android releases only.

Each release includes the signed APK, its SHA-256 checksum, and build provenance. Download the APK only from a release page, then verify its checksum before installing.

The Near source repository and development workflow are intentionally not mirrored here.